~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
					                       Assignment 1,2,3
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
>>>>
Project structure:
Project Root
│
├── Controllers
│   └── TaskController.cs
│
├── DAL
│   ├── TaskDbContext.cs          <-- Auto-generated by Scaffold
│   └── Tasks.cs                   <-- Auto-generated by Scaffold
│
├── Models
│   └── DTO
│       └── TaskDto.cs
│
├── Repositories
│   ├── ITaskRepository.cs
│   └── TaskRepository.cs
│
├── Views
│   └── Task
│       ├── Index.cshtml
│       ├── Create.cshtml
│       ├── Edit.cshtml
│       ├── Delete.cshtml
│       └── Details.cshtml
│
├── Program.cs
├── appsettings.json
└── TaskManager.csproj

>>>>
Create Database: Server Name: PRAKASH-I5PC\SQLEXPRESS   (local)
IF NOT EXISTS (
    SELECT name 
    FROM sys.databases 
    WHERE name = 'WiproTest'
)
BEGIN
    CREATE DATABASE WiproTest;
END

>>>>
Create SQL Server Table:
-- Use the target database
USE WiproTest;
GO

-- Check and create table if it does not exist || Insert sample data
IF OBJECT_ID('dbo.Tasks', 'U') IS NULL
BEGIN
    CREATE TABLE dbo.Tasks (
        TaskID INT PRIMARY KEY IDENTITY(1,1),
        TaskDescription NVARCHAR(255),
        StartDate DATE,
        ExpectedClosureDate DATE,
        AssignedTo NVARCHAR(100),
        CompletionStatus NVARCHAR(50)
    );
END
GO

-- Create Log Table
USE WiproTest;
GO

IF OBJECT_ID('dbo.Logs', 'U') IS NULL
BEGIN
    CREATE TABLE dbo.Logs (
        LogID INT IDENTITY(1,1) PRIMARY KEY,
        Description NVARCHAR(1000),
        LogLevel NVARCHAR(50),
        LogTime DATETIME DEFAULT GETDATE()
    );
END


--Adding sample data
USE WiproTest;
GO

INSERT INTO dbo.Tasks (TaskDescription, StartDate, ExpectedClosureDate, AssignedTo, CompletionStatus)
VALUES 
('Build Login Page', '2025-06-24', '2025-06-28', 'John Doe', 'Pending'),
('Design Database Schema', '2025-06-20', '2025-06-25', 'Jane Smith', 'In Progress'),
('API Integration', '2025-06-22', '2025-06-30', 'Rahul Kumar', 'Pending'),
('UI Testing', '2025-06-23', '2025-06-29', 'Anjali Sharma', 'Not Started'),
('Deploy to Production', '2025-06-24', '2025-06-27', 'Vikas Verma', 'Completed');

>>>>
Create a new project:
ASP.NET Core Web App (Model-View-Controller) || .Net 8

>>>>
Install below nuget Packages
Microsoft.EntityFrameworkCore.SqlServer
Microsoft.EntityFrameworkCore.Tools
Microsoft.EntityFrameworkCore.Design

>>>>
Scaffold the DAL - (Tools → NuGet Package Manager → Package Manager Console) and run:
Scaffold-DbContext "Server=PRAKASH-I5PC\SQLEXPRESS;Database=WiproTest;Trusted_Connection=True;TrustServerCertificate=True;" Microsoft.EntityFrameworkCore.SqlServer -OutputDir DAL -Context TaskDbContext -Tables Tasks -f

Above creates 2 files inside DAL folder: Task.cs & TaskDbContext.cs


